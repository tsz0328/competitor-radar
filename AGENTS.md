# Project Instructions

## Code index mirror

代码索引镜像位于 `D:\tool\code-index\tsz0328-competitor-radar`。

查找、搜索和阅读项目代码时，优先使用 `jcodemunch` 或该索引镜像。如果 `jcodemunch` MCP 工具不可用，则回退到当前项目 `D:\project\competitor-radar` 直接读取源码。
